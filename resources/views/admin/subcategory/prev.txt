@extends('admin.includes.main')

@section('content')
    <div class="px-2 container-xxl flex-grow-1 container-p-y">

        <div class="card">
            
            <div class="card-header d-flex align-items-center justify-content-between">
                <h5 class="mb-0 card-title text-primary">All Subcategories</h5>
                @include('admin.subcategory.create')
            </div>

            <div class="card-body">
                <table class="table table-bordered data-table" style="width:100%">
                    <thead>
                        <tr>
                            <th>No</th>
                            <th>Subcategory Name</th>
                            <th>Category Name</th>
                            <th>Status</th>
                            <th width="100px">Action</th>
                        </tr>
                    </thead>
                    <tbody>
                    </tbody>
                </table>
            </div>
        </div>


    </div>
@endsection

@section('scripts')
    <script src="https://cdn.datatables.net/1.11.4/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.11.4/js/dataTables.bootstrap5.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        $(document).ready(function() {
            $(".data-table").DataTable({
                processing: true,
                serverSide: true,
                ajax: {
                    url: "{{ route('admin.product.subcategory.index') }}",
                    beforeSend: function() {
                        $(".data-table tbody").html(
                            '<tr><td colspan="4" class="text-center">Loading...</td></tr>');
                    },
                },
                columns: [{
                        "data": "DT_RowIndex"
                    },
                    {
                        "data": "subcategory_name"
                    },
                    {
                        "data": "category_id"
                    },
                    {
                        "data": "status"
                    },
                    {
                        "data": "action",
                        sortable: false
                    },
                ],
                dom: "<'grid grid-cols-12 gap-5 px-6 mt-6'<'col-span-4'l><'col-span-8 flex justify-end'f><'#pagination.flex items-center'>><'min-w-full't><'flex justify-end items-center'p>",
                columnDefs: [{
                    targets: [3,4], // Assuming "Action" is the fourth column (index starts from 0)
                    width: '18%', // Set the desired width
                }, ],
                paging: true,
                ordering: true,
                info: false,
                searching: true,
                lengthChange: true,
                lengthMenu: [10, 25, 50, 100],
                language: {
                    lengthMenu: "Show _MENU_ entries",
                    paginate: {
                        previous: "<span class='mdi mdi-chevron-left'></span>",
                        next: "<span class='mdi mdi-chevron-right'></span>"
                    },
                    search: "Search:"
                },
                bDestroy: true,
            });

            $(document).on('click', '.deleteBtn', function() {
                var id = $(this).data('id');
                var deleteRoute = $(this).data('route');
                Swal.fire({
                    title: 'Are you sure?',
                    text: 'You will not recover this category details!',
                    icon: 'warning',
                    showCancelButton: true,
                    confirmButtonColor: '#d33',
                    cancelButtonColor: '#3085d6',
                    confirmButtonText: 'Yes, delete it!'
                }).then((result) => {
                    if (result.isConfirmed) {
                        $.ajax({
                            url: deleteRoute,
                            method: 'DELETE',
                            data: {
                                _token: '{{ csrf_token() }}'
                            },
                            success: function(response) {
                                if (response.status === 200) {
                                    Swal.fire(
                                        'Deleted!',
                                        'Category has been deleted.',
                                        'success'
                                    ).then((result) => {
                                        if (result.isConfirmed || result
                                            .isDismissed) {
                                            $('.data-table').DataTable().ajax
                                                .reload();
                                        }
                                    });
                                } else {
                                    Swal.fire(
                                        'Error!',
                                        'Failed to delete.',
                                        'error'
                                    );
                                }
                            },
                            error: function(xhr, status, error) {
                                Swal.fire(
                                    'Error!',
                                    'Failed to delete category.',
                                    'error'
                                );
                            }
                        });
                    }
                });
            });

        });
    </script>
   
@endsection
